import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF48426D); // background
  static const Color surface = Color(
    0xFF312C51,
  ); // dark surface, secondary button
  static const Color accent = Color(0xFFFEA962); // orange accent, main button
  static const Color brandText = Color(0xFFF1AA9B); // headings/text
  static const Color brandRed = Color(
    0xFF990F02,
  ); // extra action button (add/delete/archive)
  static const Color onDark = Colors.white; // button and input text on dark
  static const Color inputBorder = Color(0xFFFEA962); // border for text fields
}
